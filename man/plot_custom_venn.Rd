% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_custom_venn.R
\name{plot_custom_venn}
\alias{plot_custom_venn}
\title{Draw a custom scaled Venn diagram}
\usage{
plot_custom_venn(
  x,
  scale = c("auto", "raw", "manual"),
  max_radius_ratio = 2.5,
  manual_radius = NULL,
  manual_area_factor = NULL,
  label = c("count", "prop", "both", "none"),
  fill = NULL,
  alpha = 0.4,
  border_color = "black",
  border_linewidth = 0.7,
  set_label_color = "black",
  set_label_size = 5,
  set_label_fontface = "bold",
  set_label_family = "",
  region_label_color = "black",
  region_label_size = 5,
  region_label_fontface = "plain",
  region_label_family = "",
  region_label_fit = c("auto", "fixed"),
  region_label_min_size = 1,
  label_positions = NULL,
  set_label_positions = NULL,
  overlap = 0.62,
  prop_accuracy = 0.1
)
}
\arguments{
\item{x}{A named list of two or three vectors.}

\item{scale}{Circle scaling mode. \code{"auto"} keeps raw area-derived radius
proportions until they exceed \code{max_radius_ratio}, then compresses them.}

\item{max_radius_ratio}{Maximum visual radius ratio in auto mode.}

\item{manual_radius}{Optional manual radii for \code{scale = "manual"}.}

\item{manual_area_factor}{Optional manual area factors for \code{scale = "manual"}.}

\item{label}{Region label mode. \code{"count"} shows counts only; \code{"prop"} and
\code{"both"} show \verb{counts\\\\n(prop)}; \code{"none"} hides region labels.}

\item{fill}{Fill colors for sets.}

\item{alpha}{Fill transparency.}

\item{border_color}{Circle border colors.}

\item{border_linewidth}{Circle border line widths.}

\item{set_label_color, set_label_size, set_label_fontface, set_label_family}{Set-name text styling.}

\item{region_label_color, region_label_size, region_label_fontface, region_label_family}{Region text styling.}

\item{region_label_fit}{Region label sizing policy. \code{"auto"} tries to keep
\code{region_label_size} by adjusting the visual circle layout, then shrinks
only labels that still cannot fit. \code{"fixed"} keeps the requested circle
layout and \code{region_label_size}; unresolved issues are reported as warnings.}

\item{region_label_min_size}{Minimum region label size used by
\code{region_label_fit = "auto"} when local shrinking is needed.}

\item{label_positions}{Optional data frame with \code{region}, \code{x}, and \code{y}.}

\item{set_label_positions}{Optional data frame with \code{set}, \code{x}, and \code{y}.}

\item{overlap}{Fraction of pairwise radius sums used as center distances.
For three sets, input order maps to fixed positions: first set left-top,
second set right-top, third set bottom.}

\item{prop_accuracy}{Accuracy passed to \code{\link[scales:percent_format]{scales::percent()}} for prop labels.}
}
\value{
A ggplot object. Region label placement issues caused by fixed font
size constraints are recorded in \code{attr(plot, "custom_venn_label_issues")}.
The function warns when any such issue is present.
}
\description{
Draw a custom scaled Venn diagram
}
